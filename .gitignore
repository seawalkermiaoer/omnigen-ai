# Environment secrets
.env
.env.local
.env.*.local

# Go 后端配置——持有 jwt.secret / app_encryption_key / database.password /
# bootstrap.admin_password 四个密钥，绝不提交。server/config.yaml.example
# 是提交进仓库的无密钥示例，不受本条影响。
server/config.yaml

# Dependencies
node_modules/

# OS files
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo

# Logs
*.log
npm-debug.log*

# Qoder
.qoder/

.claude/

.trellis/

AGENTS.md

api.md
.gstack/

.superpowers/

# 部署配置，持有真实密钥（对应 deploy/config.example.yaml）
deploy/config.yaml

# 部署用配置（docker-compose.yml 挂载的就是它）。持有全部四个密钥，
# 且本仓库是公开的 —— 少这一条就等于把管理员口令和加密密钥推上 GitHub。
/config.yaml
